name: tester_external_dart
description: A tester plugin written in dart for testing external plugins
version: 1.0.0

environment:
  sdk: ^3.11.3

dependencies:

dev_dependencies:
  build_runner: ^2.4.15
  build_web_compilers: ^4.1.6
  lints: ^6.0.0
